
{{> header-logged-in}}
{{>sidenav}}
<div class="dd-matches-container">
    <h4>Your Profile</h4>
	<div id="user-info" style="max-width: 400px;">
		Email: 
		<input value="{{user.email}}" disabled type="text" >
		
		<br>
		Password: 
		<input value="{{user.password}}" disabled type="password" >
		
	</div>
</div>
